name: "pace main tests"

# Run these these whenever ...
on:
  pull_request: # ... a PR is opened / updated
  merge_group: # ... the PR is added to the merge queue
  push:
    branches:
      - main # ... when merging into the main branch

jobs:
  pace_unit_tests:
    # TODO
    # To be reverted once https://github.com/NOAA-GFDL/pace/pull/151 is merged
    uses: romanc/pace/.github/workflows/main_unit_tests.yaml@romanc/ci-debug
    with:
      component_trigger: true
      component_name: pySHiELD
